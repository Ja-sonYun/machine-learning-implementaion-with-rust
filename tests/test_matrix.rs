#[cfg(test)]
mod matrix {
    use ml::maths::n_matrix::{Matrix};

    #[test]
    fn validate_zero() {
        println!("sd");
        assert_eq!(true, true);
    }

    // fn create_matrix() {
    //     let matrix = Matrix::<f64>::new(3, 4);
    // }
}
